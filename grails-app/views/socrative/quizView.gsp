<g:each var="question" in="${questions}">
    <p>
        ${question.question}
        ${question.answer1}
        ${question.answer2}
        ${question.answer3}
        ${question.answer4}
    </p>
</g:each>
test